<Window x:Class="NotionPad.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:NotionPad.Views"
        Title="Notion Pad"
        Width="1100" Height="720" MinWidth="720" MinHeight="480"
        Background="{StaticResource BgBrush}"
        WindowStartupLocation="CenterScreen">
  <Window.Resources>
    <views:SyncStateToBrushConverter x:Key="StateBrush" />
    <views:SyncStateToLabelConverter x:Key="StateLabel" />

    <Style x:Key="TabItemStyle" TargetType="ListBoxItem">
      <Setter Property="Padding" Value="0" />
      <Setter Property="Margin" Value="0,0,2,0" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="VerticalContentAlignment" Value="Stretch" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="TabBorder" Background="Transparent"
                    CornerRadius="6,6,0,0" Padding="12,6,8,6" MinWidth="120" MaxWidth="180">
              <ContentPresenter VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="TabBorder" Property="Background" Value="#2a2a2a" />
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource BgBrush}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <SolidColorBrush x:Key="MenuPopupBrush" Color="#252525" />
    <SolidColorBrush x:Key="MenuItemHoverBrush" Color="#3a3a3a" />

    <Style x:Key="MenuButtonStyle" TargetType="MenuItem">
      <Setter Property="Padding" Value="10,4" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style TargetType="Menu">
      <Setter Property="Background" Value="{StaticResource BgBrush}" />
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <ControlTemplate x:Key="TopLevelHeaderTemplate" TargetType="MenuItem">
      <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
        <Grid>
          <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"
                            VerticalAlignment="Center" HorizontalAlignment="Center"
                            TextElement.Foreground="{StaticResource TextBrush}" />
          <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsSubmenuOpen}"
                 AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
            <Border x:Name="SubmenuBorder" SnapsToDevicePixels="True"
                    Background="{StaticResource MenuPopupBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    Padding="2" MinWidth="180">
              <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
            </Border>
          </Popup>
        </Grid>
      </Border>
      <ControlTemplate.Triggers>
        <Trigger Property="IsHighlighted" Value="True">
          <Setter TargetName="Border" Property="Background" Value="{StaticResource MenuItemHoverBrush}" />
        </Trigger>
        <Trigger Property="IsSubmenuOpen" Value="True">
          <Setter TargetName="Border" Property="Background" Value="{StaticResource MenuItemHoverBrush}" />
        </Trigger>
      </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SubmenuItemTemplate" TargetType="MenuItem">
      <Border x:Name="Border" Background="Transparent" Padding="6,4" CornerRadius="3">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock x:Name="CheckMark" Grid.Column="0" Text="✓"
                     Foreground="{StaticResource TextBrush}"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Visibility="Collapsed" />
          <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True"
                            VerticalAlignment="Center" Margin="4,0,12,0"
                            TextElement.Foreground="{StaticResource TextBrush}" />
          <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}"
                     Foreground="{StaticResource MutedBrush}" FontSize="11"
                     VerticalAlignment="Center" Margin="20,0,4,0" />
        </Grid>
      </Border>
      <ControlTemplate.Triggers>
        <Trigger Property="IsHighlighted" Value="True">
          <Setter TargetName="Border" Property="Background" Value="{StaticResource MenuItemHoverBrush}" />
        </Trigger>
        <Trigger Property="IsChecked" Value="True">
          <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
        </Trigger>
        <Trigger Property="IsEnabled" Value="False">
          <Setter Property="Opacity" Value="0.5" />
        </Trigger>
      </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="MenuItem">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      <Setter Property="Background" Value="Transparent" />
      <Style.Triggers>
        <Trigger Property="Role" Value="TopLevelHeader">
          <Setter Property="Template" Value="{StaticResource TopLevelHeaderTemplate}" />
        </Trigger>
        <Trigger Property="Role" Value="SubmenuHeader">
          <Setter Property="Template" Value="{StaticResource SubmenuItemTemplate}" />
        </Trigger>
        <Trigger Property="Role" Value="SubmenuItem">
          <Setter Property="Template" Value="{StaticResource SubmenuItemTemplate}" />
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style TargetType="Separator" x:Key="{x:Static MenuItem.SeparatorStyleKey}">
      <Setter Property="Height" Value="1" />
      <Setter Property="Margin" Value="6,4" />
      <Setter Property="Background" Value="{StaticResource BorderBrush}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Separator">
            <Border Background="{TemplateBinding Background}" Height="1" />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Tab strip -->
    <Border Grid.Row="0" Background="#1e1e1e" Padding="8,6,8,0">
      <DockPanel LastChildFill="True">
        <TextBlock DockPanel.Dock="Left" Text="📝" FontSize="14"
                   VerticalAlignment="Center" Margin="2,0,8,4" Foreground="#7fb3e8" />

        <Button x:Name="AddTabButton" DockPanel.Dock="Right" Content="+"
                Width="28" Height="28" Margin="6,0,0,4"
                Background="Transparent" Foreground="{StaticResource TextBrush}"
                BorderThickness="0" FontSize="16" Cursor="Hand"
                ToolTip="New note (Ctrl+T)" Click="OnCreateNote">
          <Button.Template>
            <ControlTemplate TargetType="Button">
              <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="4">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="b" Property="Background" Value="#2a2a2a" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Button.Template>
        </Button>

        <ListBox x:Name="TabsList" Background="Transparent" BorderThickness="0"
                 ItemContainerStyle="{StaticResource TabItemStyle}"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                 SelectionChanged="OnNoteSelected">
          <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
              <VirtualizingStackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
          </ListBox.ItemsPanel>
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Title}" FontSize="12"
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                <Ellipse Grid.Column="1" Width="7" Height="7" Margin="6,0,0,0"
                         VerticalAlignment="Center"
                         Fill="{Binding SyncState, Converter={StaticResource StateBrush}}"
                         ToolTip="{Binding SyncState, Converter={StaticResource StateLabel}}" />
                <Button Grid.Column="2" Content="✕" Margin="6,0,0,0"
                        Width="18" Height="18" Padding="0"
                        Background="Transparent" BorderThickness="0"
                        Foreground="{StaticResource MutedBrush}" FontSize="10"
                        Cursor="Hand" Click="OnCloseTab"
                        Tag="{Binding Id}" ToolTip="Close (deletes note)">
                  <Button.Template>
                    <ControlTemplate TargetType="Button">
                      <Border x:Name="b" Background="Transparent" CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                      </Border>
                      <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                          <Setter TargetName="b" Property="Background" Value="#3a3a3a" />
                        </Trigger>
                      </ControlTemplate.Triggers>
                    </ControlTemplate>
                  </Button.Template>
                </Button>
              </Grid>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </DockPanel>
    </Border>

    <!-- Menu bar -->
    <Menu Grid.Row="1" Background="{StaticResource BgBrush}" Padding="4,2"
          BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
      <MenuItem Header="File" Style="{StaticResource MenuButtonStyle}">
        <MenuItem Header="New note" InputGestureText="Ctrl+T" Click="OnCreateNote" />
        <MenuItem Header="Close note" InputGestureText="Ctrl+W" Click="OnCloseActiveTab" />
        <Separator />
        <MenuItem Header="Settings..." Click="OnOpenSettings" />
        <Separator />
        <MenuItem Header="Exit" Click="OnExit" />
      </MenuItem>
      <MenuItem Header="Edit" Style="{StaticResource MenuButtonStyle}">
        <MenuItem Header="Undo" InputGestureText="Ctrl+Z" Command="ApplicationCommands.Undo" />
        <MenuItem Header="Redo" InputGestureText="Ctrl+Y" Command="ApplicationCommands.Redo" />
        <Separator />
        <MenuItem Header="Cut" InputGestureText="Ctrl+X" Command="ApplicationCommands.Cut" />
        <MenuItem Header="Copy" InputGestureText="Ctrl+C" Command="ApplicationCommands.Copy" />
        <MenuItem Header="Paste" InputGestureText="Ctrl+V" Command="ApplicationCommands.Paste" />
        <Separator />
        <MenuItem Header="Select all" InputGestureText="Ctrl+A" Command="ApplicationCommands.SelectAll" />
        <MenuItem Header="Retry sync" Click="OnRetry" />
      </MenuItem>
      <MenuItem Header="View" Style="{StaticResource MenuButtonStyle}">
        <MenuItem x:Name="WordWrapMenuItem" Header="Word wrap" IsCheckable="True" IsChecked="True"
                  Click="OnToggleWordWrap" />
        <Separator />
        <MenuItem Header="Zoom in" InputGestureText="Ctrl++" Click="OnZoomIn" />
        <MenuItem Header="Zoom out" InputGestureText="Ctrl+-" Click="OnZoomOut" />
        <MenuItem Header="Restore default zoom" InputGestureText="Ctrl+0" Click="OnZoomReset" />
      </MenuItem>
    </Menu>

    <!-- Editor -->
    <Grid Grid.Row="2">
      <TextBox x:Name="EditorBox"
               Background="{StaticResource BgBrush}"
               Foreground="{StaticResource TextBrush}"
               CaretBrush="{StaticResource TextBrush}"
               BorderThickness="0" Padding="24,18"
               FontSize="14" FontFamily="Consolas"
               AcceptsReturn="True" AcceptsTab="True"
               TextWrapping="Wrap" SpellCheck.IsEnabled="True"
               VerticalScrollBarVisibility="Auto"
               IsEnabled="False"
               TextChanged="OnEditorTextChanged"
               SelectionChanged="OnEditorSelectionChanged" />
      <TextBlock x:Name="EmptyHint" Style="{StaticResource MutedText}"
                 HorizontalAlignment="Center" VerticalAlignment="Center"
                 IsHitTestVisible="False"
                 Text="Click + to create a new note" />
    </Grid>

    <!-- Status bar -->
    <Border Grid.Row="3" Background="#1e1e1e" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
      <Grid Margin="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" x:Name="LnColText" Style="{StaticResource MutedText}"
                   Padding="12,6" Text="Ln 1, Col 1" />
        <TextBlock Grid.Column="1" x:Name="CharCountText" Style="{StaticResource MutedText}"
                   Padding="12,6" Text="0 characters" />
        <TextBlock Grid.Column="2" Style="{StaticResource MutedText}"
                   Padding="12,6" Text="Plain text" />

        <StackPanel Grid.Column="4" Orientation="Horizontal" Margin="12,0">
          <Ellipse x:Name="StateDot" Width="8" Height="8" VerticalAlignment="Center"
                   Fill="{StaticResource MutedBrush}" Margin="0,0,6,0" />
          <TextBlock x:Name="StateText" Style="{StaticResource MutedText}"
                     VerticalAlignment="Center" Padding="0,6" Text="Idle" />
        </StackPanel>

        <Button Grid.Column="5" x:Name="SettingsButton" Content="⚙"
                Padding="10,4" Background="Transparent" Foreground="{StaticResource MutedBrush}"
                BorderThickness="0" Cursor="Hand" ToolTip="Settings"
                Click="OnOpenSettings" />

        <TextBlock Grid.Column="6" x:Name="ZoomText" Style="{StaticResource MutedText}"
                   Padding="12,6" Text="100%" />
        <TextBlock Grid.Column="7" Style="{StaticResource MutedText}"
                   Padding="12,6,16,6" Text="UTF-8" />
      </Grid>
    </Border>
  </Grid>

</Window>
